# dex-arbitr 项目说明

版本：与仓库代码同步（2026-08-31）  
跨所永续价差套利：主逻辑是**滑动窗口追 STEP**（相对 μ，有符号 ±N）。阶段 1 双腿市价；阶段 2 邻档限价先挂后吃（见 [滑动窗口-阶段2-对称挂限价.md](滑动窗口-阶段2-对称挂限价.md)）。扫描环按 [扫描方案.md](扫描方案.md) 做全市场发现与 σ−Δ 排序。小额验证见 [实盘验证.md](实盘验证.md)。字段见 [配置参考.md](配置参考.md)。安装见 [部署文档.md](部署文档.md)。接入新所见表 [新增DEX对齐清单.md](新增DEX对齐清单.md)。策略规格见 [滑动窗口-阶段1-追STEP.md](滑动窗口-阶段1-追STEP.md)。

| 文档 | 内容 |
|------|------|
| [本系统套利流程.md](本系统套利流程.md) | 本仓库启动、三条环、格子、容量校验、执行、滤网 |
| [参考项目套利流程.md](参考项目套利流程.md) | `crypto-trading-open-main` 三条入口：监控 V2 / 分段网格 / V3 |
| [参考项目V3套利流程.md](参考项目V3套利流程.md) | 参考 V3 基础模式：调度、价差/费率开仓、三点全平（源码） |
| [../web/参考项目.html](../web/参考项目.html) | 参考终端监控布局原型（分段网格 / V3 / 监控 V2） |
| [套利流程对比.md](套利流程对比.md) | 逐项有/没有、刻意不同 |
| [平仓判据备选方案B.md](平仓判据备选方案B.md) | **已废弃**。曾用往返净利闸门；现回退方案 A |
| [扫描方案.md](扫描方案.md) | **已落地**：扫描发现全市场交集、24h 量门、粗筛、窗口 σ−Δ、每币最优两所、Top N 展示 |

---

## 1. 做什么

在 **Lighter 主网**（USDC）、**Lighter Robinhood**（USDG）、**SoDEX**（vUSDC / `BTC-USD`）和 **Entropy**（Hyperliquid HIP-3，`dex=io`）的永续订单簿之间做价差对冲：一侧开多、一侧开空，锁住可成交价差。稳定币按 **1:1**（`BTC-USDC` ≡ `BTC-USDG` ≡ `BTC-USD`）。热路径不搬家、不兑币、不跨链。

Lighter 主网和 RH 共用一个适配器、两份 venue 配置。SoDEX 见 `src/exchange/sodex.rs`，Entropy 见 `src/exchange/entropy.rs`。扫描按 **所的两两组合** 比价（四所时 C(4,2)=6 条所对）。

当前 `config/default.yaml`：**`execution.enabled: false`、`scan.enabled: false`**，启动后走事件格子环。HTTP 开着时是否交易看页面「启动套利」。`paper_trading: false` 且 `monitor_only: false` 时经 Go **`exchange_sidecar`** 发真实单。阶段 2 邻档限价（yaml `order.style=limit_then_market`）是现网套利路径；阶段 1 热路径仍强制双腿市价。  
启动用 `.\target\release\dex-arbitr.exe`，不要 `cargo run`。  
`scan.enabled: true` 且执行关闭，或页面点「启动扫描」：走 [扫描方案.md](扫描方案.md)（24h 量门 + 粗筛 + 窗口 σ−Δ），不下单。执行与扫描互斥。

| 阶段 | 状态 | 内容 |
|------|------|------|
| 扫描 | 可用 | `/api/scan/start`，量门 + 粗筛 + σ−Δ Top-N，扫描页真实行 |
| 决策/执行 | 可用 | 统一决策环、滑动窗口 STEP、固定每格数量、paper / live、journal |
| 监控页 | 可用 | HTTP API + Vue 页（`web/dist`，须用进程地址打开，默认 `127.0.0.1:8090`） |
| 实盘 | 可用 | sidecar 下单；资金费率是滤网，不是独立开仓路径 |

---

## 2. 架构与目录

单 crate、单进程。页面不直连交易所。新交易所只加适配器 + 配置，Controller 流程不变。

```
启动 main
  → 读 config/default.yaml + venues
  → Controller：bootstrap 建 channel；load_available_pairs 拉各所交集（不订 WS）
  → 有 HTTP：页面勾选交易对后点「启动套利」才 activate_pairs 并订阅
  → 无 HTTP：立刻按 yaml pairs.enabled 激活
  → execution.enabled: true
        WS 写内存 → 每 100ms 统一环：先平后开 → 格子 → 容量校验 → HedgePlan
        → paper：内存持仓；live：exchange_sidecar 下单 + journal
  → execution.enabled: false && scan.enabled: true
        WS 只写内存 → 每 50ms 毛价差 Top-N → 日志
  → 两者都 false（当前默认）
        每条盘口事件走格子 / nat / res
```

完整步骤见 [本系统套利流程.md](本系统套利流程.md)。

```
dex-arbitr/
├── config/default.yaml + venues/*.yaml
├── scripts/exchange_sidecar/        # Go 实盘（Lighter / SoDEX / Entropy）
├── web/                             # Vue 3 + Naive UI 源码；构建到 web/dist
├── web/参考项目.html                # 参考终端布局原型，不接本进程 API
├── docs/                            # 流程、对比、配置、部署
├── src/main.rs                      # 读配置、起 Controller
├── src/app/controller.rs            # 三分叉 + process_pair
├── src/domain/grid.rs               # Δ 反推、Intent
├── src/domain/window_grid.rs        # 有符号 STEP
├── src/exec/                        # 规划 / 双市价（阶段 1） / 先挂后吃（阶段 2）
├── src/exchange/                    # Lighter / SoDEX / Entropy / sidecar bridge
└── src/infra/api.rs                 # /api/* + 静态页
```

另：`src/bin/live_test.rs` — 单所小额验证 CLI（account / markets / fill-pnl / market / cancel / recap）。

正式密钥文件 `config/venues/*.yaml`（含 `entropy.yaml`）**不进 git**。没有正式文件时，`load_venue` 回退到对应 `*.example.yaml`（`cargo test` 依赖这个）。

---

## 3. 端到端流程

启动后按配置三分叉（优先级：`execution` > `scan` > 事件格子）。逐步说明见 [本系统套利流程.md](本系统套利流程.md)；与参考项目逐项对照见 [套利流程对比.md](套利流程对比.md)。

当前默认不是扫描：`execution` / `scan` 都关，走 §3.3 事件格子。打开 `execution.enabled` 才是 §3.2 统一执行环。

### 3.1 扫描发现

HTTP 面板点「启动扫描」走 `/api/scan/start`（不校验套利交易对和每格数量）。细则见 [扫描方案.md](扫描方案.md)。

```
勾选 ≥2 所
  → 各所 list_perps，解析 24h 合约名义；缺字段的所本轮剔除
  → volume_24h_usdc > scan.min_volume_24h_usdc 才进 match_all_pairs
  → REST 粗筛（缺盘口则分批短订 WS）；合法盘口按点差之和截 candidate_cap
  → 长订候选，并对候选币补订宇宙里同币的其它所（填各 DEX 列均值）
  → 1Hz 入窗，满 scan.window_samples 后按 σ−Δ 打分
  → 每币留 edge 最大的两个所，截 watch_top，推 snap.scan.rows
```

扫描不下单。与「启动套利」互斥，执行优先。未满窗不占 Top N。该所没有这个币、没过量门、没盘口或未满窗，DEX 列显示 —。

### 3.2 统一执行环（`execution.enabled: true`，P1.5）

对齐参考项目 **检测 → 决策 → 执行 → 仓位** 主路径。`paper_trading: true` 模拟成交；`paper_trading: false` 经 **exchange_sidecar** 实盘下单并写 journal。

```
启动（load_available_pairs 拉交集；activate_pairs 后才订 WS）
  → 周期性 refresh 各所余额/持仓（bridge account；失败时用 sizing.fallback_available_usdc）
  → 每 loop_interval_ms（默认 100ms）：
      ① 有持仓的 pair 优先（先平后开）
      ② 从 pairs.enabled 读固定每格数量 base_qty（未配置则跳过）
      ③ books_tradable / best_sequenced_spread
      ④ SQLite 采样 + nat；res = net − max(nat, 0)
      ⑤ WindowGridEngine.decide → Open / Close / Hold
      ⑥ 仅 Open 做容量校验 check_capacity（保证金 + 一档深度）；Close 不拦
      ⑦ 槽位：open_count + pending ≤ max_concurrent_pairs
      ⑧ plan_hedge → **阶段 1 `force_market_taker`（双腿市价，不看 yaml `order.style`）**
      ⑨ monitor_only → 只记 skip_send
         paper_trading → 模拟成交 → HedgeExecutor → PositionStore
         live → bridge place/cancel → journal + PositionStore
```

**容量校验**（`src/app/sizing.rs` 的 `check_capacity`，不再定仓）：

数量来自配置的 `base_qty`，校验只回答「按这个量开得起吗」。不过则 skip `no_margin` / `thin_book`（面板「保证金不足」/「盘口过薄」），不改数量。

```
每所可用 = 该所稳定币余额 − 已占用保证金（reserved_margin_by_venue：qty × mid / 杠杆）
本笔占用 = base_qty × 加仓格数 × mid / 杠杆
保证金不够 → no_margin
一档数量 < base_qty / (depth_pct/100) → thin_book
```

配置页：勾选对的 `base_qty` 必须 > 0、≥ 该所对 `min_qty`、且符合 `qty_precision`，否则标红并拦保存/启动。格距由 `target_bp` + 四腿费 + 两所点差中枢平均反推，不再手填 `grid_step`。

HTTP/WSS 共用内存根证书（`src/exchange/net.rs`），避免 Linux 上每次建连扫 `/etc/ssl/certs` 把 FD 吃光。每个所一条行情循环，市场集合没变不重连。监控页短轮询 `/api/snapshot`（250ms）；HTTP/1 应答后拆连接，不用 keep-alive，避免一格面板把默认 1024 FD 打满。

挂单监视（limit-then-market 实盘；**阶段 1 热路径不走这条**，见下）：

1. 第一腿：taker 更高的所挂限价（maker），价格挂进**点差内侧** `order.maker_inside_ticks` 个 tick（买 `Ask1 − tick`、卖 `Bid1 + tick`，并保证不穿价）。贴自家 bid/ask 是队尾最被动的位置，配 2 秒超时几乎不可能成交。
2. post / hedge 在后台 task，不阻塞 100ms 决策环。执行 task 直接读**共享盘口**，不再用 spawn 时刻的快照。
3. **挂单监视排在所有盘口门槛之前**：单子挂出去就必须盯到撤单或成交，盘口变薄/过期都不能让监视停摆。
4. 开仓单价差跌破入场门槛、或超过 `limit_timeout_ms` → 置撤单标志。**平仓单不因价差变化撤**，要走完。
5. 每轮结束一定撤掉余量；撤单前后各查一次（撤单竞态窗口内成交仍对冲）。撤不掉 → 上报 `orphan_order` 并**停止重试**，写 journal 等人工处理。
6. 第一腿最多挂 `order.limit_retry_count` 轮，每轮用最新盘口重算价格，部分成交**累积**（baseline 逐轮滚动，不重复计数）。
7. 累计成交量低于第二腿最小下单量 → 反向平掉第一腿（`limit_min_qty_unmet`），不发注定被拒的单。
8. 第二腿数量 = 第一腿**累计实际成交量**；持仓回写也用实际成交量，不是计划量。
9. exchange_sidecar 超时分两档：写操作（place / cancel）**100s**，必须比 sidecar 内部的 90s 长；只读查询（account / order_status）**12s**。子进程带 `kill_on_drop`，超时会真的杀掉——否则一个「已放弃」的 place 仍可能把单发出去。

### 3.2.1 匹配对数量与交易范围（易混淆）

**进决策环的只有 `pairs.enabled` 里勾选且 `base_qty` 合法的交易对。** 启动仍加载各所交集全集（`GET /api/pairs/available`），但**不订阅**；点「启动套利」才 `activate_pairs`。不要和下面这些搞混：

| 字段 | 含义 | 不是 |
|------|------|------|
| `pairs.enabled` | 勾选的 base + 每格数量；未列入则只出现在配置表，不开仓 | — |
| `pairs.defaults` | 未单独指定时的 Δ / max_step | 交易范围 |
| `sizing.depth_pct: 50` | 单笔不得超过 L1 数量的 **50%** | 50 个交易对 |
| `scan.analysis_interval_ms: 50` | 扫描环间隔 **50ms** | 50 个交易对 |
| `scan.watch_top: 20` | 仅 **scan 模式**日志/面板 Top-N | 执行模式下的监控数量 |

四所时同一 `BTC-USD-PERP` 最多出现在 **C(4,2)=6** 条所对里。`available` 条数可以很多；真正订阅的是 enabled ∩ 所选交易所。不合格的 `base_qty`（低于 min_qty 或精度不对）该所对 skip 并打 ERROR，不整体 bail。

Web 页（`http://127.0.0.1:8090`）：

| 显示 | 来源 | 含义 |
|------|------|------|
| 配置表「可用所对」 | `/api/pairs/available` | 各所交集，按 symbol 聚合 |
| 顶栏 **匹配对** | `stats.matched_pairs` | 已激活（订阅）的内部 `Pair` 条数 |
| 表格 **共 N 个** | `snap.pairs` | 按 `pair_id` 去重后的价差行 |
| 配置页「交易所持仓」 | `snap.venue_stats` | **一行一个已加载所**。列：点差中枢（与 μ 同窗的买卖点差均值，未满显示 `n/cap`）、交易量（本次进程该所成交名义）、**签名→确认**（`sign+send=合计ms · 全链路wallms`；未下单 `—`）、持仓。`sign` = 本地签名，`send` = HTTP 收单回包，合计不含 nonce/锁/成交回查；全链路是 Rust `bridge_place`（含认成交）。Lighter / SoDEX / Entropy 同一口径，见 [新增DEX对齐清单.md](新增DEX对齐清单.md) §4.9。两所中枢的平均 \(C\) 折进 Δ |
| 顶栏「交易所持仓」 | `exchange_positions.length` | 各所原始仓**条数**（不是所的行数） |

实盘建议：配置页只勾 BTC/ETH 等少数币，并填对的每格数量；跨 DEX 可只勾 `lighter` + `entropy` 减少组合。`target_bp` 过低会把 Δ 压到只够费+点差，成交会像噪声。配置页芯片发黄只按往返费估 Δ，没有 live C。

### 3.3 事件格子路径（`scan.enabled: false` 且 `execution.enabled: false`）

```
每次一侧盘口更新
  → skip wait / stale / thin_book / invalid / no_spread
  → net = raw − fee（不预扣滑点）
  → 每 5s 把 raw 写入 SQLite；nat 优先用库里快照
  → nat = 该方向 24h raw 中位数（首次满 10 点才落快照）
  → res = net − max(nat, 0)（只给监控看）
  → 满窗且残差站住 Δ−h → WindowGridEngine → intent=open
  → 阶段 1 双腿市价；monitor_only → skip_send
```

`skip_*` 是过滤，不是错过的开仓。开仓只看连续的 `intent=open`。

---

## 4. 价差公式

全部百分比。可成交价差，**不用中间价**。

```
raw  = (卖所 Bid1 − 买所 Ask1) / 买所 Ask1 × 100
fee  = 双腿 taker                         # 阶段 1
net  = raw − fee
nat  = 该 pair + 该买卖方向、24h 内 raw 的中位数（SQLite 快照）
res  = net − max(nat, 0)
```

对齐参考 V3：决策只用卖一/买一，**不预扣滑点**。Ask1/Bid1 数量不够 `base_qty` / 两腿 `min_qty` → `thin_book`，不吃第二档。

滑点挪到成交之后，按实际成交价扣。成交价若贴着 `max_slippage_pct` 保护限价，记账改用决策 BBO（见套利流程 §6）。

`cost.default_slip_pct: 0.01` 是成交后 overrun 阈值，不再当决策成本。`0` 表示不检查。

yaml `order.style` 默认 LTM 只给阶段 2。阶段 1 不「谁先挂」。

---

## 5. 格子与平仓条件

### 5.1 开仓：相对 μ 追 STEP

旧分段网格（绝对 T1、一次开满 n 格、剥头皮）已删除。现为滑动窗口：满 `window_samples` 点才有 μ 和各所点差中枢；空仓只 `0→±1`，点差窗未满也不开。Δ 由 `target_bp` + 四腿市价费 + 两所 live 点差中枢的平均 + 滞后反推。详见 [滑动窗口-阶段1-追STEP.md](滑动窗口-阶段1-追STEP.md)。

`net` / `res` 只给监控和扫描上榜用，不进 `WindowGridEngine::decide`。

### 5.2 平仓：STEP 朝 μ 收回

加仓 raw ≥ k+1−h，减仓 raw ≤ k−1+h。过零必须先回 0。**没有**往返止盈闸、**没有**往返止损、**没有**剥头皮。`round_trip_pct` 仍写入 journal / 面板，只作记录。

盈利性由 Δ 相对该所对往返市价费和点差中枢平均自己扛。`h=0.25` 时一格只锁 0.5Δ（当前 yaml `h=0` 锁整格）；不要把 `target_bp` 设到扣完四腿费和两所点差平均后仍接近 0。

平仓视角仍要算出来（往返记录、一档厚度）；拿不到 → Hold。一档撑不住**本笔**平仓量 → 丢掉格子平仓意图（`thin_book`）。

L=`legs[0]`，R=`legs[1]`；k>0 空 L 多 R。

### 5.3 状态按「币 + 所对」隔离

三所两两组合下同一个 `pair_id` 有 C(3,2)=3 条 `Pair`。持仓、挂单、持续性计时全部按 `slot_key(pair_id, venue_a, venue_b)` 索引，不是 `pair_id`。

`max_segments` 默认 **3**（= max_step），`target_bp` 默认 `1`。持续性用 `persistence_ms` 时间窗；`persistence_min_hits` > 0 时窗内累计达标次数够了就过，为 0 则连续不掉线。

---

## 6. 下单规划与执行（不发真实单）

### 6.1 规划（`exec/planner.rs`）

阶段 1 热路径：**开仓条件仍是窗口 μ + 持续性 + 跨格**（`symmetric_limit=false`）。`force_market_taker` 改 `plan.style`，两腿同时市价；`run_plan` 跟 plan，不跟 yaml `order.style`（页面默认仍是 LTM）。勾上对称挂单则本拍不走这条。

阶段 2（已落地，见 [滑动窗口-阶段2-对称挂限价.md](滑动窗口-阶段2-对称挂限价.md)）走 `limit_then_market`：

1. 第一腿：费率更便宜的所挂邻档限价（maker）。买 ≤ bid−n tick、卖 ≥ ask+n tick（n=`maker_inside_ticks`，至少 1）  
2. 第一腿成交后：另一所市价。点**停止**后听到成交不再对冲 / 紧急平  
3. 邻档不按 `limit_timeout_ms` 撤；事件撤见阶段 2 §4.4（对面先成 / 0.2Δ 改价 / 边没了）。**不**因 1 tick 改价撤  
4. F=0 **只拦阶段 2**（面板「费率未加载」）；yaml 免费档 `"0"` 同样不挂。阶段 1 不走这条  
5. 输掉邻档竞态：紧急平多余腿后冷却 3 秒。有 Foreign 时开仓档和减仓档都不挂  
6. 面板「邻档 n/2」只表示内存 pending；DEX 有单要看日志 `limit_market: first leg posted`

### 6.2 执行（`exec/executor.rs`）

| 模式 | 行为 |
|------|------|
| `monitor_only: true` | 只到规划 + 日志（`skip_send`）。**不登记挂单状态**，所以不会占住决策环 |
| `paper_trading: true` | 走完整执行链，按当前 BBO 模拟两腿成交，更新内存持仓；不调交易所 |
| `paper_trading: false` | 经 `exchange_sidecar` 真实下单 |

阶段 1 双市价：两腿同时 `place`，各自认成交。都成按 `min` 记账；一腿确认、一腿确定没成才 `emergency_close` 已成的那腿；任一腿认不到则不再发。阶段 2：限价确认后第二腿失败才平第一腿；第二腿不明不平第一腿。回滚每次等该单推送 `ioc_fill_wait_ms`（默认 1 秒，页面可改 2/3 秒）再查一次，最多 3 次。仍平不掉则记 `NAKED_FIRST_LEG`。`hedge_failed_legs` 只补 BotFailure（**必须仍开着套利**）。Foreign 仓不自动补对冲。

`monitor_only` 与 `paper_trading` 都不会留下未清理的挂单状态——旧实现登记完 `pending` 就返回，
而 `pending` 只在执行回调里清理，于是第一个 open 信号就把整个决策环永久卡死。

---

## 6.5 可靠性守卫

热路径还拦：盘口新鲜度、合法 BBO、一档深度、保证金、人工介入。阶段 2 另拦 F=0 邻档、Foreign、竞态冷却。`risk` 整段（价格稳定性、费率闸、限额、reduce-only 探针、错误退避）已拆，后续重做。

### 6.5.1 人工介入状态机（`app/intervention.rs`）

用于「内存持仓已不可信」的情形。四个触发点：

| 触发 | 说明 |
|------|------|
| 单腿裸仓连续 3 次 | `SINGLE_LEG_STREAK_LIMIT`；两腿干净成交则连击归零 |
| orphan 挂单撤不掉（失败路径） | 残单状态不明，可能稍后成交 |
| orphan 挂单撤不掉（**成交成功但仍留单**） | 容易漏——成交了不等于没留下残单 |
| 紧急平仓失败 | 第一腿裸着且平不掉 |

**开仓和平仓都挡**。介入态意味着记账本身不可信，按错的量去平会把敞口放大。

两条兜底防止永久锁死：

- **30 分钟自动解除**（`AUTO_RESUME`）
- **格数变化自动解除**——持仓量变了说明对账已经跟上

挂起瞬间打 `ERROR` 并写 journal；之后每轮只累加 `intervention` 跳过计数并降为 `debug`，避免刷屏。状态按 pair 隔离，一个币挂起不影响其他币。

---

## 7. 配置字段

### 7.1 `config/default.yaml`

| 字段 | 当前值 | 含义 |
|------|--------|------|
| `system.monitor_only` | `false` | `true` 只到规划 + 日志（`skip_send`），不发单 |
| `system.data_freshness_ms` | `3000` | 一侧盘口超过该毫秒未更新 → `stale` |
| `system.log_dir` | `data/logs` | 文本日志按天滚动。空 = 只 stderr。`DEX_LOG_DIR` 可覆盖 |
| `venues` | `[lighter, lighter_rh, sodex, entropy]` | 各所两两扫描 |
| `pairs.defaults.*` | `target_bp: 1`，`max_segments: 3` | 未单独指定时的 STEP 默认。Δ 运行时 = (目标% + F + C)/(1−2h)，C 为两所中枢平均 |
| `pairs.enabled` | BTC `0.001`、ETH `0.02` | 进决策环的交易对；必填 `base_qty`。空 = 只监控不交易 |
| `pairs.enabled[].overrides` | BTC 的 `[lighter, entropy]` 密档 | 按所对覆盖 `target_bp` / `max_segments`；**不能**覆盖 `base_qty` |
| `execution.enabled` | `false` | `true` → 统一决策环；与纯 scan 互斥 |
| `execution.loop_interval_ms` | `100` | 统一环分析间隔 |
| `execution.paper_trading` | `false` | `true` 模拟成交；`false` 走 exchange_sidecar 实盘 |
| `sizing.max_concurrent_pairs` | `1` | 最大同时持仓 + 挂单占槽对数 |
| `sizing.leverage_multiplier` | `5` | 容量校验用的杠杆倍数 |
| `sizing.depth_pct` | `50` | 单笔不得超过 L1 数量的这个比例（%） |
| `sizing.margin_utilization_pct` | `90` | 可用保证金使用比例（%），留 buffer |
| `sizing.leverage_by_venue` | （可选） | 单所杠杆覆盖，如 `sodex: "3"` |
| `sizing.refresh_balance_secs` | `1` | 余额和交易所持仓刷新间隔（秒） |
| `sizing.fallback_available_usdc` | `500` | bridge 余额失败时的回退可用 USDC |
| `live_test.dex_test_mode` | `false` | `true` 时主流程下单受 `max_qty` 截断（DEX 联调） |
| `live_test.max_qty` | `0.001` | live-test CLI 与 dex_test_mode 时的单笔上限 |
| `live_test.journal_path` | `data/executions.sqlite` | 执行带 / recap |
| `http.enabled` | `true` | 监控 API + `web/dist`（不是 `web/src`） |
| `http.bind` | `127.0.0.1:8090` | 监听地址。`auth_token` 为空时**拒绝**非回环绑定，自动回落到 127.0.0.1 |
| `http.auth_token` | （空） | 非空则 `/api/*` 需 Bearer token。要公网访问必须设 |
| `scan.enabled` | `false` | 页面「启动扫描」打开；`execution` 优先，二者互斥 |
| `scan.analysis_interval_ms` | `50` | 扫描拍间隔（入窗仍 1Hz） |
| `scan.watch_top` | `20` | 扫描表最多几行（按币） |
| `scan.window_samples` | yaml `120` | 满这么多个秒级点才打分；与格子窗分开 |
| `scan.min_volume_24h_usdc` | yaml `"3000000"` | 单所该币 24h 名义下限；写成 0 时仍按 1000 万 |
| `scan.coarse_refresh_secs` | `60` | 粗筛重跑。刷新结果为空则保留当前候选 |
| `scan.max_own_spread_pct` | `"0.15"` | 现码不作粗筛硬门，点差之和只用于候选排序 |
| `scan.min_level_notional_usdc` | `"200"` | 现码不作粗筛硬门 |
| `grid.persistence_ms` | `1000` | 检查时间窗（毫秒） |
| `grid.persistence_min_hits` | `5` | 窗内累计达标次数；0 = 连续不掉线 |
| `grid.window_samples` | yaml `1000` | 满窗才有 μ 和各所点差中枢（折进 Δ）。代码默认 10000 |
| `grid.step_hysteresis` | yaml `"0"`（代码缺省 0.25） | 加/减仓错开；必须 &lt; 0.5。0 = 满格才开、回到 μ 才平 |
| `grid.symmetric_limit` | `false` | `true` = 阶段 2 邻档。F=0 时阶段 2 不挂、阶段 1 仍开 |
| `grid.quote_reprice_ratio` | `"0.2"` | 空仓 μ 偏这么多 ×Δ 才撤邻档重挂 |
| `grid.min_quote_gap_ratio` | `"0.3"` | 加仓档离格线够远才新挂 |
| `order.style` | `limit_then_market` | yaml 默认。阶段 1 发单跟 plan（双市价），不看此项 |
| `order.limit_timeout_ms` | `2000` | 追逐型限价单轮最长等待（非邻档） |
| `order.adjacent_timeout_ms` | `0` | 邻档第一腿存活；0 = 只按事件撤。面板「邻档 n/2」≠ DEX 已挂 |
| `order.ioc_fill_wait_ms` | `1000` | 市价 / IOC / 撤后等该单 WS；没有再 REST 查一次。可改 2000/3000，页面可热改 |
| `order.maker_inside_ticks` | `1` | 阶段 1：点差内侧。阶段 2 邻档：离本所 bid/ask 再退开 n tick |
| `order.limit_retry_count` | `3` | 第一腿最多挂几轮（每轮重算价格、累积成交） |
| `cost.default_slip_pct` | `0.01` | 成交后实际滑点 overrun 阈值（%）。决策不预扣 |
| `history.enabled` | `true` | 打开 SQLite：样本 + `nat` 快照 |
| `history.db_path` | `data/spreads.sqlite` | 相对工作目录。重启直接读快照 |
| `history.sample_interval_secs` | `5` | 同一方向最少间隔 |
| `history.window_hours` | `24` | 中位数窗口 |
| `history.min_points` | `10` | 首次落快照的最少样本；已有快照不受此限 |
| `history.refresh_interval_secs` | `1800` | 有快照后隔这么久用窗口样本重算（30 分钟） |
| `history.max_age_secs` | `200` | 兼容旧配置，读路径不再用它过期快照 |

百分比在 yaml 里写成 `"0.03"` 表示 **0.03%**，不是 3%。`risk` 整段已删除。

执行带平仓列 **本次 bp** = `pnl_pct` × 100（`0.02` = 2 bp），用本笔两腿成交价往返，不是所方累计已实现。成交价不能是滑点保护限价，见 [本系统套利流程.md](本系统套利流程.md) §6。进程只提供 `web/dist`：改 `web/src` 后必须 `cd web && npm run build`；`app.js` 无 hash，浏览器可能要 Ctrl+F5。

### 7.2 启用 paper 决策环（示例）

仅联调决策/容量校验/内存持仓，**仍不发真实单**：

```yaml
execution:
  enabled: true
  paper_trading: true
scan:
  enabled: false
system:
  monitor_only: false
sizing:
  fallback_available_usdc: "200"
pairs:
  enabled:
    - symbol: BTC
      base_qty: "0.001"
```

HTTP 开着时也可不改 yaml：配置页勾选 BTC、填每格数量，勾 paper，点启动。

### 7.3 初期小额实盘（示例）

见 [实盘验证.md](实盘验证.md)。要点：

```yaml
execution:
  enabled: true
  paper_trading: false
scan:
  enabled: false
system:
  monitor_only: false
pairs:
  enabled:
    - symbol: BTC
      base_qty: "0.001"
live_test:
  dex_test_mode: false   # 主流程走 pairs.enabled 的每格数量，不受 max_qty 截断
http:
  enabled: true
  bind: "127.0.0.1:8090"
```

稳定后加大 `base_qty` / 格数，并确认 `sizing.leverage_multiplier` 与账户保证金匹配。

### 7.4 `config/venues/*.yaml`

| 字段 | 主网 | RH | SoDEX | 含义 |
|------|------|----|-------|------|
| `id` | `lighter` | `lighter_rh` | `sodex` | 必须与 `venues` 列表一致 |
| `rest` / `ws` | 主网 URL | `api.rh.lighter.xyz` | `mainnet-gw.sodex.dev` | REST 拉市场，WS 订簿 |
| `chain_id` | `304` | `466324` | `623` | 签名用；监控可不填对 |
| `quote` | `USDC` | `USDG` | `USDC` | 匹配时都归一成 USD |
| `fees.maker` / `fees.taker` | `0.005` / `0.005` | `0.012` / `0.035` | `0.012` / `0.040` | 单位 %。按账户档位改 |
| `account_index` / `account_id` | 账户号 | **独立账户** | `account_address` | Lighter 用 index；SoDEX 填主钱包地址 |
| `api_key_index` | 一般 `0` | 一般 `0` | — | Lighter 用 |
| `api_key_name` | — | — | `SODEX_API_KEY_NAME` | SoDEX 的 `X-API-Key` 是名称 |
| `api_key_private_key` / `private_key` | 私钥 | 私钥 | `SODEX_PRIVATE_KEY` | 空或 `0x` = 未加载，监控仍可跑 |

`market_index` **不写在配置里**：Lighter 从 `orderBooks` 拉，SoDEX 用 symbols 的 `id`。SoDEX 盘口走 `allBookTicker`（BBO），不是 L2 全簿。

---

## 8. 控制台字段

扫描模式：控制台和 `data/logs/` 打同一套代币行，不画面板。Windows 建议直接跑编译好的 exe。

### 8.0 扫描模式（页面「启动扫描」）

结果在扫描页表格（`snap.scan.rows`），按有效振幅 σ−Δ 排序，不是控制台毛价差榜。页头：宇宙 / 候选 / 满窗 / 展示。未满 `window_samples` 时倒计时。

各 DEX 列是该币在该所的 mid / 点差窗口均值（满窗才有数）。最优所对以外的所也会采，只要宇宙里有这个币并订到了盘口。

拷回日志后可看 `scan volume gate`、`scan universe matched`、`scan coarse filter done`、`raised/shrunk memory position`。

### 8.1 格子 / 统一执行环统计行（非 scan 模式）

```
decide  hold=2709  open=0  close=0  skip_send=0
filter  stale=4  thin=0  wide=0  spread=0  size=0  base=0  cancel_gone=0  cancel_to=0
```

| 字段 | 含义 |
|------|------|
| `hold` | 有完整价差，但不够开/平 |
| `open` / `close` | **决策层**：格子本轮给出开仓 / 平仓 |
| `skip_send` | **执行层**：已经做出计划，但 `monitor_only` 没发到交易所 |
| `stale` | 一侧盘口超过 `data_freshness_ms` |
| `thin` | 一档数量不够 `base_qty` / 两腿 `min_qty`。空仓挡开仓；有仓时格子本笔平仓量不够也记这条并丢掉平仓意图 |
| `wide` | （已不用）旧单所点差上限已随 `risk` 拆掉 |
| `spread` | 算不出可成交方向（`no_spread`） |
| `size` | 容量校验不过（`no_margin` 保证金不够 / `thin_book` 一档太薄）。不再定仓，数量来自 `base_qty` |
| `base` | 拿不到第一腿 baseline 持仓，放弃本轮实盘执行（**不会**用 0 兜底） |
| `cancel_gone` | 挂单未成且价差不够，已置撤单标志 |
| `cancel_to` | 挂单超时，已置撤单标志 |
| `intervention` | 该 pair 处于人工介入态，开仓和平仓都挡（见 6.5.1） |

`open`/`close`/`hold` 和 `skip_send` **不是互斥分桶**。同一轮：格子说开仓 → `open+1`，接着因监控不下单 → `skip_send+1`。所以监控时常见 `open` 与 `skip_send` 一起涨，价差行仍显示 `intent=open`，表示「识别到了，故意没发」。

`wait`、`invalid_bbo` 会计入 skip，不一定单独出现在这一行。

### 8.2 盘口行

`venue` `pair` `bid` `ask` `bq` `aq`（后两个是买一/卖一数量）  
`pair` 是标准化 id（`BTC-USD-PERP`），不是所方原始名。

### 8.3 价差行

第一行：`pair` `buy` `sell` `intent` `pts`  
第二行：`raw` `slip` `net` `nat` `res`

| 字段 | 含义 |
|------|------|
| `pair` | `BTC-USD-PERP` |
| `buy` / `sell` | 本轮买所 / 卖所 |
| `raw` | 可成交毛价差 % |
| `slip` | 决策为 0；成交后才按实际成交价填 |
| `net` | `raw − fee`（**未减** nat） |
| `nat` | 快照 / 24h raw 中位数；无快照显示 `--` |
| `res` | 决策用净边。无 nat 快照且空仓为 `+0` |
| `pts` | 有 nat 时是窗口样本数；否则 `已有/10`。**不是点位** |
| `intent` | `hold` / `open` / `close` / `scalp` / `scalp_tp` / `limit` / `skip` |

`intent=skip` 时看 `note`：`stale`、`thin_book`、`invalid_bbo`、`no_spread`、`wait`。

---

## 9. 核心类型

```
VenueId          "lighter" | "lighter_rh"
VenueMarket      venue, raw_symbol, pair_id, base, market_index, qty_precision, min_qty
Pair             pair_id（如 BTC-USD-PERP）+ 两腿 VenueMarket
Bbo              bid/ask/qty + 多档 bids/asks + ts
NetSpread        buy, sell, raw_pct, fee_pct, slip_pct, net_pct
Position         pair_id, buy, sell, qty, grid（内存；paper 执行环会写入）
PositionStore    持仓 + pending 占槽，max_concurrent_pairs 限制
Intent           Open { qty, buy, sell, grid } | Close { qty, grid } | Hold
HedgePlan        数量、买卖所、first/second 腿（style + is_buy）
HedgeExecutor    paper 模拟 / live 经 sidecar
LimitWatch       StillWait | CancelSpreadGone | CancelTimeout | FilledHedgeNow
ExecResult       first/second 腿模拟或真实成交摘要
```

匹配规则：只取永续；`USDC` / `USDG` / `USDT` / `USD` / SoDEX `vUSDC` 都视为 USD；`pair_id = {BASE}-USD-PERP`；任意两所同一 `pair_id` 各成一条监控腿。下单仍用所方 `raw_symbol` 和 `market_index`。

---

## 10. SQLite 与天然价差

两张表：

- `spread_samples`：`ts, pair_id, buy, sell, raw_pct, net_pct`（每 5 秒一条 raw）
- `natural_spreads`：该方向最新中位数快照（`value, points, updated_ts`）

**天然价差 `nat`** = 该 `pair_id + buy + sell` 在 24h 窗口内 **raw** 的中位数。首次至少 10 个点才写入快照。

启动：库存在且快照表有行 → **立刻用**，不用再等 10 个新点。旧库只有样本、没有快照表时，打开时按窗口回填。之后每 `refresh_interval_secs`（默认 1800 秒 / 30 分钟）用当前窗口重算并覆盖快照。窗口暂时不够 10 点时**保留旧快照**。

扫描跨 DEX 和格子路径都读写这个库。清空：停进程后删 `data/spreads.sqlite`。

---

## 11. 已实现 / 未接线

| 已实现 | 未做 / 刻意不做 |
|--------|----------------|
| 三所两两扫描、过线币关键行 | 多腿 |
| 滑动窗口追 STEP（相对 μ、有符号 ±N、`target_bp`+费+点差中枢平均反推 Δ、滞后 yaml 0） | 旧分段网格 T1/T0/剥头皮（已删） |
| 开仓看相对 μ 的残差 | 绝对 T1 |
| 阶段 1 双腿市价 + 回滚 + journal | V3 费率套利开仓、盈利点全平 |
| 盘口新鲜 / 深度 / 保证金 / 介入 | 费率/稳定性/reduce-only 滤网（已拆，后续重做） |
| paper / live 完整执行链 | 扫描结果自动晋升到执行 |
| HTTP 配置页（须走 `127.0.0.1:8090`） | sidecar 常驻化（每次调用仍 fork） |

---

## 12. 怎么判断「识别对了」

**扫描模式：** 扫描页出现满窗行，排序随 σ−Δ；页头宇宙 / 候选 / 满窗有数。SoDEX / Entropy 列在宇宙有该币且订到盘口、满窗后应有均值，不是整列 —。

**格子 / 执行环：** 看 `raw` / `net` / `nat` / `res` / `pts` / `intent`。开仓看相对 μ 的残差是否站住 Δ 并过持续性检查。paper 且非 `monitor_only` 会出现 `filled_open` / `filled_close`。

确认稳定后再接线 REST 签名下单。不要当实盘。
